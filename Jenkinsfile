node {
    stage('Checkout') {
        checkout scm
    }
    stage('Build') {
        echo "buildiso -p ${profile}"
    }
    stage('Build') {
        echo "buildiso -p ${profile}"
        sh "printenv"
    }
}
